{
  "name": "ai-brief",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI-powered pipeline for generating briefs, stories, and development plans",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "docs:dev": "npm run docs:dev --prefix docs",
    "docs:build": "npm run docs:build --prefix docs",
    "docs:preview": "npm run docs:preview --prefix docs"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
