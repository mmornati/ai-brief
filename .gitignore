node_modules/
coverage/

# Test scratch directories
test/.tmp/

# Runtime output (gitignored per architecture § Project Structure)
# The ai-brief-output/steps/.gitkeep placeholder is force-tracked to keep
# the directory present in fresh clones (git cannot re-include a file under
# an ignored parent directory via negation rules).
ai-brief-output/

# Step state markers
.step-*.completed
.step-*.failed

# VitePress documentation build output
docs/.vitepress/dist/
docs/.vitepress/cache/

# Backup files from install.sh
*.bak

# Local environment and IDE config
.env
.env.local
.env.production
.opencode/
.agents/
_bmad/
.vscode/
.idea/
*.swp
*.swo
